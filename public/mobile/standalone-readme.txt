YTDBNS iPhone 15+ launch contract

For true edge-to-edge physical-screen use on iPhone, launch YTDBNS from an iOS Home Screen shortcut / installed web app.
Normal Safari browser tabs always retain Safari-owned browser chrome and cannot be removed by page CSS.

Standalone detection:
- CSS display-mode: standalone
- navigator.standalone === true

The web app remains read-only in the browser layer.
