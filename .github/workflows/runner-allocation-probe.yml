name: Runner Allocation Probe

on:
  push:
    branches:
      - mobile-ux-redesign

permissions: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Runner heartbeat
        run: |
          echo "runner-started"
          uname -a
          node --version || true
